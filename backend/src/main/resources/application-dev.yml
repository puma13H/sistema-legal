# Perfil DEV - PostgreSQL local
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/casos_legales
    username: postgres
    password: 12345678
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
